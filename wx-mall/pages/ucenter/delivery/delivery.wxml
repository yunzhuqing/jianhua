<i-steps current="{{verticalCurrent}}" direction="vertical">
    <i-step wx:for="{{infos}}">
        <view slot="title">
              {{item.time}}
        </view>
        <view slot="content">
            {{item.desc}}
        </view>
    </i-step>
</i-steps>
